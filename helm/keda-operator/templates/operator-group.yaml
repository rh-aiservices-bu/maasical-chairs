apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ .Values.operatorGroup.name }}
  namespace: {{ .Values.namespace.name }}
spec:
  upgradeStrategy: {{ .Values.operatorGroup.upgradeStrategy }}