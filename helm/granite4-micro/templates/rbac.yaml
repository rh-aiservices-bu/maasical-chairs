{{- if .Values.keda.enabled }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: keda-autoscaling-metrics-{{ include "granite4-micro.fullname" . }}
  labels:
    {{- include "granite4-micro.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - monitoring.coreos.com
    resources:
      - prometheuses
      - prometheuses/api
    verbs:
      - get
      - list
      - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: keda-autoscaling-metrics-{{ include "granite4-micro.fullname" . }}
  labels:
    {{- include "granite4-micro.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: keda-autoscaling-metrics-{{ include "granite4-micro.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: inference-prometheus-auth
    namespace: {{ .Release.Namespace }}
{{- end }}
