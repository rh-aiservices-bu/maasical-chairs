apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: {{ include "granite4-micro.fullname" . }}
  labels:
    {{- include "granite4-micro.labels" . | nindent 4 }}
    opendatahub.io/dashboard: 'true'
    monitoring.opendatahub.io/scrape: 'true'
    opendatahub.io/genai-asset: 'true'
  annotations:
    openshift.io/display-name: {{ .Values.inferenceService.displayName | default "granite4-micro" }}
    serving.knative.openshift.io/enablePassthrough: 'true'
    {{- if and .Values.keda.enabled .Values.httpAddon.enabled }}
    serving.kserve.io/autoscalerClass: external
    {{- else if .Values.keda.enabled }}
    serving.kserve.io/autoscalerClass: keda
    {{- end }}
    serving.kserve.io/deploymentMode: RawDeployment
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "8080"
  finalizers:
    - serving.kserve.io/inferenceservice-finalizer
spec:
  predictor:
    {{- if and .Values.keda.enabled .Values.httpAddon.enabled }}
    maxReplicas: {{ .Values.httpAddon.maxReplicas | default (.Values.inferenceService.maxReplicas | default 1) }}
    minReplicas: {{ .Values.httpAddon.minReplicas | default 0 }}
    {{- else }}
    maxReplicas: {{ .Values.inferenceService.maxReplicas | default 1 }}
    minReplicas: {{ .Values.inferenceService.minReplicas | default 1 }}
    {{- end }}
    {{- if and .Values.keda.enabled (not .Values.httpAddon.enabled) }}
    autoScaling:
      metrics:
        - type: External
          external:
            metric:
              backend: "prometheus"
              serverAddress: "https://thanos-querier.openshift-monitoring.svc.cluster.local:9091"
              query: >
                {{ .Values.keda.query | default "sum(vllm:num_requests_waiting{model_name=\"granite4-micro\"})" }}
            target:
              type: Value
              value: {{ .Values.keda.threshold | default 2 }}
            authenticationRef:
              authModes: bearer
              authenticationRef:
                name: inference-prometheus-auth
    {{- end }}
    model:
      modelFormat:
        name: {{ .Values.inferenceService.modelFormat | default "vLLM" }}
      name: {{ .Values.inferenceService.modelName | default "" }}
      resources:
        {{- toYaml .Values.resources | nindent 8 }}
      runtime: {{ include "granite4-micro.fullname" . }}
      storageUri: {{ .Values.inferenceService.storageUri | default "oci://quay.io/redhat-ai-services/modelcar-catalog:granite4-micro" }}
    tolerations:
      {{- toYaml .Values.tolerations | nindent 6 }}
    nodeSelector:
      {{- toYaml .Values.nodeSelector | nindent 6 }}
